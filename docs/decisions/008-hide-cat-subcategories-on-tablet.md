# ADR 008: 태블릿 cat 하위 카테고리 비노출

- 상태: 승인
- 날짜: 2026-02-28

## 배경

- 태블릿에서 홈 `cat` 영역에 하위 카테고리가 노출되어 루트 중심 탐색 의도와 달랐다.

## 결정

- 태블릿 구간(768~1024px)에서 `#pageHome .catHub .catSubGrid`, `#pageHome .catStack .catStackSubs`를 강제로 숨긴다.

## 결과

- 태블릿 `cat` 영역은 루트 카테고리만 노출된다.
- 모바일/PC의 기존 카테고리 동작은 유지된다.
