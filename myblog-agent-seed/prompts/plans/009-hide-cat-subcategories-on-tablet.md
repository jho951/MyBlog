# 태블릿 cat 하위 카테고리 숨김

목표:
- 태블릿에서 홈 `cat` 영역의 하위 카테고리를 비노출 처리한다.

작업 지시:
- `src/style/style.css`의 태블릿 미디어쿼리(768~1024px)에 하위 카테고리 숨김 규칙을 추가한다.

검증 기준:
- 태블릿에서 `catSubGrid`/`catStackSubs`가 보이지 않는다.
- 루트 카테고리 카드만 노출된다.
