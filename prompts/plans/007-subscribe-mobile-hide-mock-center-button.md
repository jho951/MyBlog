# 모바일 구독 섹션 간소화

목표:
- 모바일에서 구독 영역의 mock 카드를 숨기고 CTA 버튼을 중앙 정렬한다.

작업 지시:
- `src/style/style.css`에 모바일 미디어쿼리(`max-width: 767px`)를 추가한다.
- `.subscribePromo__mock`을 숨긴다.
- `.btn_subscription`을 중앙 정렬한다.

검증 기준:
- 모바일에서 mock 카드가 보이지 않는다.
- 구독 버튼이 중앙에 위치한다.
